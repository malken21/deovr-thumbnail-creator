name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  build:
    runs-on: ${{ matrix.runner }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows
            arch: x64
            runner: windows-latest
          - os: windows
            arch: arm64
            runner: windows-11-arm
          - os: macos
            arch: x64
            runner: macos-latest
          - os: macos
            arch: arm64
            runner: macos-latest
          - os: linux
            arch: x64
            runner: ubuntu-latest
          - os: linux
            arch: arm64
            runner: ubuntu-24.04-arm

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Remove existing release assets
        uses: mknejp/delete-release-assets@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ github.ref_name }}
          assets: |
            latest.yml
            latest-mac.yml
            latest-linux.yml
        continue-on-error: true

      - name: Build and Release
        shell: bash
        run: |
          case "${{ matrix.os }}" in
            windows)
              npm run package -- --publish always --win --${{ matrix.arch }}
              ;;
            macos)
              npm run package -- --publish always --mac --${{ matrix.arch }}
              ;;
            linux)
              npm run package -- --publish always --linux --${{ matrix.arch }}
              ;;
          esac
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

